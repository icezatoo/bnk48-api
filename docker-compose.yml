version: '3'
services:
  mongodb:
    image: 'mongo'
    ports:
      - '27017:27017'
  mongodb_test:
    image: 'mongo'
    ports:
      - '27018:27017'
  mongo-seed:
    build: ./mongo-seed
    links:
      - mongodb
  mongo-seed-test:
    build: ./mongo-seed
    links:
      - mongodb_test
    environment:
      - HOST_DB="mongodb_test"
